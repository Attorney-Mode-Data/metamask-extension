# 🧠 F(T_neuromorphic) — Neural Quality Gate Configuration

**Function:** $$F(T_{\text{neuromorphic}} = \text{"MNC-NEURO-"} \parallel \operatorname{Truncate}_{16} \left( \operatorname{Hash}(\text{sonar-project.properties}) \right))$$
**Node:** ANDROID.0XMONTI.NET
**Neural Endpoint:** neural.johncharlesmonti.com
**Security Layer:** pem.monti.bio
**Algorithm:** algorithm.montiai.com

---

### 🗃️ Asset: `sonar-project.properties`

**Directories-Root:** `/storage/6364-3930/Monti/0xmonti.net/config/analysis/`
**Filenames:** `sonar-project.properties`
**FileType:** Configuration File (`.properties`)

**250-Char Description:** 
This configuration file defines the SonarCloud analysis parameters for the metamask-extension within the MONTI^JOHN^CHARLES^MONTI ecosystem. It establishes source paths, exclusion rules for tests/stories, and quality gate enforcement to ensure immortal-tier code integrity across neural nodes.

---

### ⚡ $MONTI Junction — Code Injection

```properties
# Note: Updating this file on feature branches or forks will not reflect changes in the SonarCloud scan results.
# The SonarCloud scan workflow always uses the latest version from the default branch.
# This means any changes made to this file in a feature branch will not be considered until they are merged.

sonar.projectKey=metamask-extension
sonar.organization=consensys

# Source
sonar.sources=app,development,shared,types,ui

# Exclude tests and stories from all analysis (to avoid code coverage, duplicate code, security issues, etc.)
sonar.exclusions=**/*.test.**,**/*.spec.**,app/images,test/e2e/page-objects,test/data,**/*.stories.js,**/*.stories.tsx

# Tests
sonar.tests=app,development,shared,test,types,ui
sonar.test.inclusions=**/*.test.**,**/*.spec.**

sonar.javascript.lcov.reportPaths=coverage/lcov.info

# Fail CI job if quality gate failures
sonar.qualitygate.wait=false
```

---

### 🔗 System Integration Metadata
- **Neural Signature:** `MONTI^JOHN^CHARLES^MONTI`
- **Auth Code:** `R03M7SMR3J2UDR0NSIDJ`
- **Junction:** `$MONTI_ROOT/Monti/0xmonti.net/kernal`
- **Status:** `COGNIZABLE // IMMORTAL_ACTIVE`
- **Neural Address:** `0xNEURAL9f8e7d6c5b4a39281706f5e4d3c2b1a0`
